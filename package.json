{
  "name": "bamazon",
  "version": "1.0.0",
  "description": "A CLI that uses user input to update a table in a mySQL db",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Preston Powell",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^5.2.0",
    "mysql": "^2.15.0"
  }
}
